interface Props {
    id: string
};

const NoteDetails: React.FC<Props> = ({ id }) => {
    return (
        <header>
            <h1>Título</h1>
        </header>
    )
};

export default NoteDetails; 